# Manual additions / overrides for funding opportunities.
#
# Merged with whatever sources/commonfund.py scrapes; dedupe key is `id`.
# If an opportunity is missing from the public listing or its scrape fails,
# add it here.
#
# Schema matches cfde_atlas_etl.models.opportunity.CommonFundOpportunity:
#
# - id (required)
# - prefix (required: RFA / NOT / OTA)
# - activity_code (optional; "" if unknown)
# - source_url (required; can be a stub URL if added manually)

opportunities: []
